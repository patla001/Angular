/*
 * ContentTabBarEnum
 * Enum describing the tabs for the content-tab-bar-popup component
 * The File tab accepts images along with file matching the allowable
 * extensions.
 */

export enum ContentTabBarEnum {
    FILE, // File includes images
    LINK,
    TEXT,
    HTML
}
